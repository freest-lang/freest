<pending>

Usage tightening for type-abstraction binders in expression position.

Awaiting: `*T` tightening for `\@a -> ...` binders in the body of a value
definition when a value of the abstract type is discarded/duplicated. The forcing
runs on function-clause parameters (`kindFun'`), not expression lambdas
(`kindExp`).

Literal fully-stripped duplicate of Valid/SystemF/SystemFBooleans/SystemFBooleans.fst; the annotated original passes.
