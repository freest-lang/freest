# Linearity

The **Linear and unrestricted values** and **Treading carefully with linear functions** sections should come first.

## Functions with multiple parameters

(highlights) it highlight**s** the type of expression linBinApply h

# Channels and session types

## Exchanging values and closing channels

**send** returns only the channel endpoint, not a pair

function writeFive''; should be writeFive'''

## A summary of the basic elements of interaction

(example of sendType) [a/Int] should be [Int/a]

# Abstract types

## Session existentials and universals

incTwice (2nd implementation) is annotaded on the second receive but later it is written "None of the various occurrences of receive is annotated" and "In incTwice we managed to get away with no type annotations". The explanation also includes "Let us try annotating the first [receive]", when it's actually the second
